pluginManagement {
    repositories {
        mavenCentral()
        jcenter()
        mavenCentral()
        google()
        maven { url = uri("https://jitpack.io") }
    }
}
rootProject.name = "Sovchi"
include(":app")
 